name: Event_Filters
on:
  push:
    # This code ignores the main branch commits
    branches-ignore:
      - main
 
jobs:
  linux-os:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4.2.2
      - run: echo "Filters test"
 

    
